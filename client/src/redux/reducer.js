const initialState = {
    pokemons: [],
    allPokemons: [],
    currentPage: 1,
    types: [],
    detail: [],
    error: "",
  };

const rootReducer = (state = initialState, action) => {
    return state
}

export default rootReducer;